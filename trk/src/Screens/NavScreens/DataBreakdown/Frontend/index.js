import React, { useState, useEffect } from "react";
import { Text, View, ScrollView, SafeAreaView, StyleSheet, Image } from "react-native";

const DataBreakdown = ({route}) => {
  return (
    <ScrollView>
    <SafeAreaView>
      <View >
          <Text >More detailed breakdown</Text>
        </View>
    </SafeAreaView>
  </ScrollView>
)
}

export default DataBreakdown